{
  "name": "opus.ro",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:holding": "SITE_MODE=holding astro dev",
    "build": "SITE_MODE=holding astro build && node scripts/prune-holding.mjs dist && node scripts/check-output.mjs dist",
    "build:full": "SITE_MODE=full astro build && node scripts/check-output.mjs dist",
    "preview": "astro preview",
    "check": "astro check",
    "og": "node scripts/make-og.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/source-serif-4": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "astro": "^7.1.6",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
